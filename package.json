{
  "name": "gemini-news-netlify",
  "version": "1.0.0",
  "description": "Gemini News Service on Netlify",
  "scripts": {
    "build": "echo 'No build step required'",
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^15.0.0"
  }
}
